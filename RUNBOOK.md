# RUNBOOK — التشغيل اليومي

الملف هذا موجّه لـ **Claude اللي شغال على جهاز المستخدم** (اللي عنده TradingView MCP).

المستخدم ما يحتاج ينسخ تعليمات من أي مكان. يكفي يقول:

> اتبع RUNBOOK.md

وكل اللي تحت ينفّذ بالترتيب، بدون ما يسأل عن كل خطوة.

---

## قبل أي شيء

```bash
git pull
```

**لا تصلح باجات بنفسك قبل ما تتأكد إنها موجودة فعلاً في النسخة الحالية.**
تحقق أولاً، وبعدين قرر:

```bash
python -c "from crypto_agent.schema import coerce_number; print(coerce_number('−0.53'))"
```

- طبع `-0.53` → علامة الناقص U+2212 متعامَل معها. **لا تضيف أي تحويل يدوي**
  في سكربت الجمع، ولا تعدّل `schema.py`. التحويل اليدوي يخفي هل المحلل
  الحقيقي شغال ولا لا.
- طبع خطأ → قل للمستخدم، ولا تصلح بدون إذنه.

```bash
python -m pytest tests/ -q
```

أي فشل هنا يتقال للمستخدم قبل أي تحليل. تقرير مبني على كود فاشل أسوأ من لا تقرير.

---

## الفحص

**الواتش ليست فيها 35 رمز.** لا تمر عليها رمزاً رمزاً عبر تفكيرك — هذا يستهلك
حصة الجلسة كاملة في فحص واحد. اكتب سكربت جمع مرة واحدة يمر على الرموز ويكتب
snapshot واحد على القرص، وأعد استخدامه في كل فحص.

لو الجلسة قريبة من حد الاستخدام، قل ذلك وافحص فئة `major` فقط بدل ما تبدأ فحصاً
كاملاً ما تقدر تكمّله. فحص ناقص يُعرض كأنه كامل أسوأ من فحص صغير.


1. **Escape** — اقفل أي قائمة أوامر مفتوحة على الشارت. فيها أوامر شراء حقيقية.
2. اتبع `.claude/skills/crypto-spot-agent/SKILL.md` حرفياً.
3. شغّل:
   ```bash
   python -m crypto_agent watch <snapshot.json>
   ```

**الإشعارات:** المستخدم وافق على إرسال تنبيهات Telegram — هذا هو الغرض من
إعداد البوت. شغّل الأمر **بدون** `--no-notify` إلا إذا طلب غير ذلك صراحةً.

---

## المؤشرات

حساب TradingView **Basic يسمح بمؤشرين فقط**. الأولوية:

| # | المؤشر | ليه |
|---|---|---|
| 1 | **RSI** | المحرك ما يقدر يشتقه |
| 2 | **MACD** | نفس الشيء |
| — | EMA | **لا تصرف خانة.** المحرك يحسبها من نفس الشموع |
| — | Ichimoku | **لا تصرف خانة.** المحرك يحسبها من الشموع مع الإزاحة |

لو الخانتين مشغولتين بـ Ichimoku و RSI، قل للمستخدم إن MACD ضايع وإن
Ichimoku ما يحتاج خانة أصلاً.

---

## الرموز اللي ما تحمّلتش

بعض الرموز تتغير أسماؤها أو تُشطب (MATIC صار POL، FTM صار S). لو رمز ما حمّل:

- **قلها للمستخدم بالاسم.** لا تتجاهله بصمت — الصمت يُقرأ كأنه "ما في إشارة".
- اقترح تعديل `config/watchlist.json`، ولا تعدّله بنفسك بدون إذن.

## عرض النتيجة

اعرض مخرجات المحرك **كما هي**. لا تعيد صياغة مستوى، ولا تضيف مستوى ما طبعه.

- **"No setup met the criteria"** → نتيجة صحيحة، مو عطل. قلها بوضوح.
- **رفض بسبب السحابة** → اذكر حدود السحابة، لأنها تقول للمستخدم ينتظر إيش.
- **رفض بسبب التكاليف** → اذكر الرقم بالـ R.

**ممنوع تخفيف أي حد في `config/watchlist.json` عشان تطلع إشارة.**
الحدود موجودة لأن backtest حقيقي على منصة سابقة للمستخدم خسر بسبب التكاليف.
لو ما في صفقة، الجواب "ما في صفقة".

---

## بعد الفحص

قارن أسعار الجمع بالأسعار الحالية. لو تحرك السعر بشكل ملحوظ أثناء التحليل، قلها.

لو المستخدم نفّذ صفقة بنفسه:

```bash
python -m crypto_agent open --symbol BINANCE:XXXUSDT --entry <سعر التنفيذ الفعلي> --size <الكمية> --stop <الستوب> --target <الهدف>
```

**سعر التنفيذ الحقيقي، مو المقترح.** الفرق بينهما هو الانزلاق الفعلي، وهو معلومة.

---

## ممنوع منعاً باتاً

- تنفيذ أي صفقة. لا `replay_trade`، ولا نقر على تذكرة أمر، ولا شيء.
- افتراض أي رقم لم يُسحب فعلاً.
- وصف إشارة بأنها "مؤكدة" أو "مثبتة" — المحرك غير متحقق منه بعد.

---

## لو المستخدم سأل "شنو ندير دابا"

جاوب بحسب الحالة، بسطر أو سطرين، بدون شرح طويل:

- **ما في صفقة** → "السوق ما فيه إعداد. لا شيء تعمله. الفحص الجاي بعد ساعة."
- **في صفقة مقترحة** → اعرضها، وقل له يقارنها بالشارت قبل أي تنفيذ.
- **في صفقة مفتوحة** → اعرض حالتها بالـ R وكم باقي على الستوب.
